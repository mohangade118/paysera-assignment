# Copy to .env.local when running php bin/console on the host (outside Docker).
# Inside the php container, MESSENGER_TRANSPORT_DSN is set by docker-compose instead.
###> symfony/messenger ###
MESSENGER_TRANSPORT_DSN=amqp://paysera:paysera@127.0.0.1:5672/%2f/messages
###< symfony/messenger ###
